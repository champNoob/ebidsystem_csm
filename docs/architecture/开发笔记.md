# 开发笔记

## 编码阶段

### 特例：handler 调用 model

`handler` 层 `internal\api\handler\order_handler.go` 中调用了 `model` 层的函数：

```go
role, err := model.ParseUserRole(roleStr)
```

`ParseUserRole` 是系统的输入防火墙之一，负责把外部字符串世界，转换为内部安全的领域类型
